# 2026-07-12 工作日志

## 阅读：ChatGPT 导出的「项目功能集成方案」聊天记录（10 条消息）
- 文件：`C:\Users\ai\Downloads\202607121510-chatgpt-项目功能集成方案-10msg.md`
- 核心：用户确认要把 AIComicBuilder 二开成 AI 短视频平台（类似 Pixelle-Video），本地媒体生成用 ComfyUI（Z-Image 文生图 + IndexTTS2 声音克隆），文本走 LLM，合成走 FFmpeg。
- 确认当前 `ai-m` 代码仍是纯原版漫剧，二开未开始；`agents/` 是 bailian/coze/dify 平台 agent 导出，与短视频无关。
- 已将此长期背景写入 `MEMORY.md`。

## 解读结论
- 这是一份**方案规划**聊天，不是已落地代码。下一步若要动手，建议从「projectType 字段 + 短视频表 + ComfyUI 通用层 + Z-Image/IndexTTS2 服务」起步。

## 评估：用户粘贴的「收敛后短视频方案」（~28 节长文）
- 用户给出收敛版：视觉主体资产层 + 音频主时间线 + 多供应商视频 + 本地 ComfyUI + 生成契约 + 一致性闭环 + 18 张新表 + 里程碑 + PR 拆分。
- 判断：方向整体高度合理。强项=音频主时间线、本地稳定关键帧+云端只运动、视觉主体抽象、参考图语义化、供应商能力解耦、不破坏原流程。
- 需警惕：第一版规模被低估；一致性是工程难点非架构可解；Grok 供应商列入第一梯队过早；本地多模型同卡串行性能；数据模型过度规范化（建议先 JSON 字段，稳定后再拆表）；需核实 Pixelle-Video dev 是否真有所述契约（勿只信转述）。
- 已给诚实评估，等用户决定下一步（落地 or 继续规划）。
