# 旧模型状态迁移说明

旧浏览器 `model-store`（模型状态存储）包含地址、密钥、协议和默认模型。迁移不得直接把全部状态上传到普通接口。

1. 管理员重新认证；
2. 服务端创建一次性迁移会话；
3. 页面只提交管理员明确选择的供应商；
4. 端点禁止请求正文日志和错误回显；
5. 服务端把密钥写入安全存储并仅保存引用；
6. 创建执行后端和生成配置；
7. 验证成功后清除浏览器密钥；
8. 提示轮换长期存储过的密钥；
9. 无法唯一映射的默认模型要求管理员选择；
10. 迁移事件写入审计，但不含秘密。
