# 变更记录

## 2.0 - 2026-07-12

### 根源性架构调整

- 新增服务端执行后端和密钥引用；
- 新增不可变工作流包和生成配置修订版；
- 新增逻辑任务、执行尝试、工件和资源池领域；
- 新增独立工作进程、数据库租约和防旧写令牌；
- 新增提交不确定状态和人工处理流程；
- 新增工作流供应链晋级和平台安全策略；
- 新增流式媒体归档和原子提交协议；
- 新增兼容迁移和回滚方案；
- 新增双轮对抗式审查报告和安全威胁模型；
- 新增严格结构约束、正向与负向夹具和自动校验。

### 第二轮修正

- 客户端任务编号必须经行为探测；
- 修复跨文件系统原子重命名假设；
- 资源槽位防旧写令牌不再重置；
- 安全策略不再由工作流包定义；
- 通用执行尝试不再硬编码特定后端编号语义；
- 后端认证结构支持多密钥引用；
- 数据库、文档和结构约束统一命名。

### 最终质量门禁

- 修复无效表格样式引用并恢复可访问表头；
- 重绘超宽信任边界图和执行状态机；
- 完成主手册 44 页和审查报告 9 页逐页视觉检查；
- 完成 `DOCX`（可编辑文档）可访问性、标题层级、宏、修订和外部关系审计；
- 完成 `PDF`（固定版式文档）字体嵌入、结构标签、脚本、附件和逐页渲染校验；
- 新增 `QUALITY_GATE_REPORT.md`（质量门禁报告）；
- 清理内部生成参考文件，补充最终文件摘要。
