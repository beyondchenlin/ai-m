console.error("This legacy seed command was removed by PR-12 because fabricated workflows are unsafe. Use `pnpm workflow:import <package-dir>` with a real ComfyUI API workflow package.");
process.exit(1);
