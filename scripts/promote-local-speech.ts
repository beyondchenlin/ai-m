console.error("This legacy auto-promotion command was removed by PR-12. Use `pnpm workflow:promote` with exact WORKFLOW_DIGEST, CONFIRM_WORKFLOW_DIGEST and EXECUTION_BACKEND_ID values; reviewer identity is derived from the authenticated Windows token.");
process.exit(1);
